package enums;

/**
 * Enumeration representing the delivery status of an email notification.
 * Tracks whether an email has been successfully sent, is pending, or failed.
 */
public enum EmailStatus {
    SENT,
    PENDING,
    FAILED
}
