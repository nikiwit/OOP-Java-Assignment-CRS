package enums;

/**
 * Enumeration representing the current state of a course recovery plan.
 * Tracks the progress of a student's recovery process.
 */
public enum RecoveryStatus {
    ACTIVE,
    COMPLETED,
    PENDING
}
