package enums;

/**
 * Enumeration representing the types of assessment components in a course.
 * Used to distinguish between different grading categories.
 */
public enum ComponentType {
    ASSIGNMENT,
    EXAM
}
