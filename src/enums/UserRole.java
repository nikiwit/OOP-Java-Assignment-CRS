package enums;

/**
 * Enumeration representing the different roles a user can have in the system.
 * Defines access levels and permissions for authentication and authorization.
 */
public enum UserRole {
    ADMIN,
    INSTRUCTOR,
    STUDENT
}
